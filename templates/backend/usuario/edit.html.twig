{% extends 'backend/base.html.twig' %}
{% block title %}Edición del usuario {{ usuario.id }}
{% endblock %}

{% block body %}
    <h1 class="display-6 mt-4">Edicion de usuario</h1>
    
    <div class="card mb-4">
        <div class="card-body">
            {{ include('backend/usuario/_form.html.twig', {'button_label': 'Actualizar'}) }}
        </div>
        <div class="card-footer">

            <a href="{{ path('app_usuario_index') }}" class="pl-2 text-secondary float-left mr-5">
                <i class="fas fa-caret-square-left"></i>Volver a lista</a>
            {{ include('backend/usuario/_delete_form.html.twig') }}

        </div>
    </div>
{% endblock %}
