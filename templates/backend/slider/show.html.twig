<!DOCTYPE html>

<title>Slider</title>

{% block body %}
    <h1>Slider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>UrlFoto</th>
                <td>{{ slider.urlFoto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slider_index') }}">Volver a la lista</a>

    <a href="{{ path('app_slider_edit', {'id': slider.id}) }}">Editar</a>

    {{ include('backend/slider/_delete_form.html.twig') }}
{% endblock %}
