<!DOCTYPE html>

<title>New Pedido</title>

{% block body %}
    <h1>Create new Pedido</h1>

    {{ include('pedido/_form.html.twig') }}

    <a href="{{ path('pedido_index') }}">back to list</a>
{% endblock %}
