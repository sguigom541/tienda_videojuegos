{% extends 'backend/base.html.twig' %}
{% block title %}Pedido {{ pedido.id }}
{% endblock %}

{% block body %}
    <h1>Pedido</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pedido.id }}</td>
            </tr>
            <tr>
                <th>FechaPedido</th>
                <td>{{ pedido.fechaPedido ? pedido.fechaPedido|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TotalCompra</th>
                <td>{{ pedido.totalCompra }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pedido_index') }}">back to list</a>

    <a href="{{ path('app_pedido_edit', {'id': pedido.id}) }}">edit</a>

    {{ include('backend/pedido/_delete_form.html.twig') }}
{% endblock %}
