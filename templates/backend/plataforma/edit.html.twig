<!DOCTYPE html>

<title>Edit Plataforma</title>

{% block body %}
    <h1>Edit Plataforma</h1>

    {{ include('plataforma/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('plataforma_index') }}">back to list</a>

    {{ include('plataforma/_delete_form.html.twig') }}
{% endblock %}
