<form method="post" action="{{ path('app_videojuego_delete', {'id': videojuego.id}) }}"
      onsubmit="return confirm('¿Estas seguro que quieres eliminar este videojuego?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ videojuego.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
